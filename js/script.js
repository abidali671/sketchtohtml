// Carusel Flickity
$("nav .main-carousel").flickity({
  // options
  cellAlign: "left",
  contain: true,
  autoPlay: 500,
});

$(".main-carousel").flickity({
  // options
  cellAlign: "left",
  contain: true,
});
